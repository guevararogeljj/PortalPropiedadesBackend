using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace UnitTests.Controllers
{
    [TestClass]
    public class PropertiesControllerTest
    {
        //private Mock<ISignInUserService>? _mockSignInService;
        //private Mock<IPropertiesService>? _mockPropertiesService;
        //private Mock<ILogger<PropertiesController>>? _mockLogger;


        //[TestMethod]
        //public void GetProperties()
        //{
        //    Assert.IsInstanceOfType(Task.Run(async () =>
        //    {
        //        this._mockSignInService = new Mock<ISignInUserService>();
        //        this._mockPropertiesService = new Mock<IPropertiesService>();
        //        this._mockLogger = new Mock<ILogger<PropertiesController>>();

        //        PropertiesController properties = new PropertiesController(this._mockPropertiesService.Object, this._mockLogger.Object);

        //        var result = await properties.RangeProperties(1, 20, null, null, null, null, null, null, null, null);

        //        var convert = ObjectToDictionary.Generate((result as JsonResult).Value);

        //        return result;

        //    }).GetAwaiter().GetResult(), typeof(JsonResult));
        //}

    }
}
